[project]
name = "spice"
version = "0.2.0"
description = "Add your description here"
license = "Apache-2.0"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.75.0",
    "google-genai>=1.55.0",
    "mcp>=1.27,<2",
    "openai>=2.14.0",
    "prompt-toolkit>=3.0.52",
    "pydantic>=2.13.4",
    "pyright>=1.1.411",
    "rich>=15.0.0",
    "ruff>=0.15.20",
    "setproctitle>=1.3.7",
    "tavily-python>=0.7.26",
    "textual>=8.2.8",
    "typer>=0.26.7",
]

[project.scripts]
spice = "spice.cli.main:app"

[tool.uv]
package = true

[tool.setuptools.packages.find]
include = ["spice*"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
    "pytest-textual-snapshot>=1.0.0",
]

[[tool.uv.index]]
url = "https://mirrors.aliyun.com/pypi/simple/"
default = true
